# Copy this file to .env and fill in your key.
# .env is gitignored, so the real key never gets committed.
#
# Free Gemini Developer API key (no billing card needed):
#   https://aistudio.google.com/apikey
#
# USE_VERTEXAI=FALSE routes ADK to the AI Studio / Gemini Developer API.
# Set it to TRUE only for the Vertex AI path (needs a billed GCP project).

GOOGLE_GENAI_USE_VERTEXAI=FALSE
GOOGLE_API_KEY=
